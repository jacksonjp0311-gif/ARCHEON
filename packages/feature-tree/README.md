# package: feature-tree

Feature records live in DesignIR (`features.json`). This package is a documentation boundary for a future typed TS feature-tree widget. The workstation currently reads features from `/api/design`.
